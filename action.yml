name: 'Semantic Release Notes Preview'
description: 'Generates and outputs a release notes preview'
branding:
  color: 'white'
  icon: 'check-circle'
outputs:
  releaseNotes:
    description: 'The release notes generated by semantic-release'
  releaseVersion: 
    description: 'The version of the release generated by semantic-release'
runs:
  using: 'node16'
  main: 'dist/index.js'

